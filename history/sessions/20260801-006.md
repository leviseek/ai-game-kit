# 输入

请读取当前 implement-framework-foundation-v1 的 proposal.md、design.md、tasks.md。

现在进入 Implementation 阶段。

请先不要修改代码。

先输出：

1. 当前将要实现的第一个任务范围
2. 会创建哪些文件
3. 每个文件的职责
4. 预计依赖关系
5. 需要我确认的架构风险

等待确认。

# 输出

## Implementation 准备状态

- Change：`implement-framework-foundation-v1`
- Schema：`spec-driven`
- 当前进度：`0/46`
- 状态：Ready
- 本轮未修改任何文件或代码。

## 1. 第一个任务范围

仅执行 `tasks.md` 的 **1.1**：

- 记录当前工具链基线：
  - Bun `1.3.13`
  - Node.js `v24.15.0`
  - 未安装 Vitest、Jest、tsx、ts-node、独立 tsc
- 建立统一命令：`bun run test:foundation`
- 创建最小测试验证 Bun 能直接运行 TypeScript。
- 执行一次受控的 Red → Green：
  1. 临时失败断言，确认命令以非零状态退出。
  2. 修正为通过断言，确认测试成功。
  3. 最终仓库只保留通过状态。
- 更新任务状态和验证证据。

本任务**不会**创建 Framework 运行时代码，也不会开始任务 1.2。

## 2. 会创建或修改的文件

### 新建

`D:\ai-work\ai-game-kit\tests\framework\foundation\baseline.test.ts`

- Bun 测试运行器的最小基线测试。
- 位于 `assets` 外，不参与 Cocos 资源导入。
- 后续 Logger、Module、Application 测试沿用该目录。

### 修改

`D:\ai-work\ai-game-kit\package.json`

- 增加 `test:foundation` 脚本。
- 不增加 dependencies/devDependencies。
- 不生成 lockfile。

`D:\ai-work\ai-game-kit\openspec\changes\implement-framework-foundation-v1\tasks.md`

- 将任务 1.1 标记完成。
- 记录工具版本、测试运行器现状和 Red/Green 验证结果。

### 不会修改

- `assets/framework/**`
- `assets/boot/**`
- `startup.scene`
- 任何 `.meta`
- `tsconfig.json`
- Cocos 的 `library`、`temp`、`profiles`
- 当前未跟踪的 `history/sessions/20260801-006.md`

## 3. 文件职责

| 文件 | 职责 |
|---|---|
| `package.json` | 提供跨会话可重复执行的 foundation 测试入口 |
| `baseline.test.ts` | 证明 Bun 测试发现、TypeScript 执行和断言门禁有效 |
| `tasks.md` | 保存任务完成状态与可审计验证证据 |

## 4. 预计依赖关系

```text
package.json:test:foundation
        ↓
      Bun CLI
        ↓
tests/framework/foundation/*.test.ts
        ↓
      bun:test
```

约束：

- 测试代码不依赖 `cc`。
- Framework 运行时代码不得依赖 Bun API。
- 不引入第三方测试依赖。
- `tasks.md` 只记录实施状态，不属于运行时依赖。

## 5. 需要确认的架构风险

1. **测试运行器绑定 Bun**  
   测试文件会使用 `bun:test`，但 Framework 源代码保持运行时无关。建议接受。

2. **修改 Creator 根 `package.json`**  
   只增加 scripts，不改 `creator`、`name`、`uuid` 和依赖。建议接受。

3. **Red 测试处理方式**  
   失败状态只用于本地验证，不提交故意失败的测试；最终仅保留 Green 状态。建议接受。

4. **Bun 测试不能替代 Cocos 类型检查**  
   Bun 主要验证纯 TypeScript 行为；Cocos 类型和引擎兼容性将在任务 1.5 及后续 Creator 验证中处理。

请确认是否按以上范围执行任务 1.1。
