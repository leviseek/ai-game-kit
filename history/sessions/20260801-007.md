# 输入

执行 tasks.md：

1.2
1.3

只完成 Framework 基础目录和公开契约。

要求：

不要实现 Application。
不要实现 ModuleRunner。
不要实现 Logger。
不要修改 startup.scene。

完成后：

输出：
1. 新增文件列表
2. 每个文件职责
3. 设计.md对应章节
4. 测试结果

等待 review。

# 输出

## 1. 新增文件列表

- `D:\ai-work\ai-game-kit\assets\framework\index.ts`
- `D:\ai-work\ai-game-kit\tests\framework\foundation\public-boundary.test.ts`

同时建立目录：

- `core/errors`
- `core/lifecycle`
- `contracts/application`
- `contracts/module`
- `contracts/logging`
- `application`
- `diagnostics/logging`
- `adapters/cocos/application`

空目录未添加 `.gitkeep` 或占位实现，也未手工创建 `.meta`。

## 2. 文件职责

### `assets/framework/index.ts`

Framework 唯一根公开入口。

当前仅声明为空模块，不提前暴露任何类型。Module、Logger、Application 契约将在对应任务实现后按需导出。

### `public-boundary.test.ts`

验证：

- Framework 根公开入口存在。
- 边界检查可以识别跨边界深层导入。
- `assets/boot`、`assets/game`、`assets/resources` 中的消费者只能使用 Framework 根入口。

此外已更新 `tasks.md`，将 1.2、1.3 标记完成并记录 Red/Green 证据。

## 3. design.md 对应章节

- **Decision 1：目录只为当前切片建立，不预建未来系统**
  - 建立规定目录。
  - 未创建 UI、Resource、Scene、ECS 或业务目录。
  - 未加入占位 Manager 或实现。

- **Decision 2：Framework 根入口只导出稳定契约和必要启动类型**
  - 建立唯一根入口。
  - 当前不暴露尚未实现的契约。
  - 通过测试禁止消费者深层导入。

- **Decision 12：纯 TypeScript 测试保留 Bun**
  - 边界测试使用 Bun。
  - 不依赖 Cocos 和第三方测试框架。

## 4. 测试结果

### RED

根入口尚未创建时：

- `2 pass`
- `1 fail`
- 失败原因：`assets/framework/index.ts` 不存在
- 深层导入检测用例已通过

### GREEN

目标边界测试：

- `3 pass`
- `0 fail`

完整 Foundation 测试：

- `4 pass`
- `0 fail`
- 共 2 个测试文件

其他验证：

- OpenSpec strict validation：通过
- OpenSpec 进度：`3/46`
- `git diff --check`：通过
- `startup.scene`：未修改
- Framework 中仅新增 `index.ts`，没有 Application、ModuleRunner 或 Logger 实现

当前修改尚未提交。等待 review。