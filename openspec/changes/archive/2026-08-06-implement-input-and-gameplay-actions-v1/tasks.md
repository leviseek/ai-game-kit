# Implement Input And Gameplay Actions v1 — Tasks

对应总计划 `create-game-framework-v1` 任务 6.4/6.5。

## 1. 输入内核测试（红期先行）

- [x] 1.1 先编写输入映射测试：覆盖绑定输入产生对应 action、未绑定输入不产生 action、同一输入在映射变更后产生不同 action。
- [x] 1.2 先编写上下文切换测试：覆盖未激活上下文不产生 action、切换立即生效、切换后旧上下文残留采样不派发。
- [x] 1.3 先编写采样测试：覆盖按下/释放状态区分、模拟输入携带连续值、时间戳取自注入单调时钟且按下到释放单调递增。
- [x] 1.4 先编写输入源替换测试：覆盖运行时替换后停止旧源事件、新源按相同映射与上下文激活状态继续产生 action。

## 2. 输入内核实现

- [x] 2.1 实现 `contracts/input/*` 纯接口：action 标识、底层输入事件、采样（状态/值/时间戳）、输入上下文与映射声明。
- [x] 2.2 实现 `core/input/*` 引擎无关内核：映射表、激活上下文门控与采样产生逻辑，使 1.x 测试通过且不依赖 `cc`。
- [x] 2.3 实现 UI/玩法阻断：接收阻断判定回调（默认消费 `UiNavigator` 模态状态），模态生效时玩法上下文不派发 action，并编写双响应测试（单次采样、不重复派发）。
- [x] 2.4 根入口白名单同步：新增输入契约与核心符号至 `expectedRootExports`，依赖边界检查通过。

## 3. Cocos 输入适配器

- [x] 3.1 实现 `adapters/cocos/input/CocosInputAdapter.ts`：订阅 `cc.input` 触摸、鼠标、键盘与可用手柄事件，翻译为内核可接收的底层输入事件，遵循引擎接缝可注入 mock 模式。
- [x] 3.2 编写适配器集成测试：真实事件路径下 action 产生正确、模态阻断时玩法不响应、解除后恢复、手柄缺失/未连接降级为无输入而非报错。
- [x] 3.3 完成依赖边界检查与 strict 类型检查：适配器允许 `cc` 导入，内核与契约不导入 `cc`/`fgui`。

## 4. 收口验证

- [x] 4.1 运行完整 Bun 单元测试与 strict TypeScript 检查，记录测试数量与零失败结果。
- [x] 4.2 执行 ADR 检查：确认本次输入实现是否产生新的架构决策；如有，按 `doc/decisions/ADR-NNN-<slug>.md` 创建 ADR；如无，明确记录无需新增 ADR。
