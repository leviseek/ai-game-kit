# Implement Config Service v1 — Tasks

对应总计划 `create-game-framework-v1` 任务 7.1/7.2。

## 1. 配置服务测试（红期先行）

- [x] 1.1 先编写类型化读取测试：覆盖按声明类型读取配置值、类型不匹配抛出类型化错误。
- [x] 1.2 先编写缺失与解析失败测试：覆盖缺失配置与解析失败分别以不同类型化错误表达、错误携带可诊断信息。
- [x] 1.3 先编写默认值策略测试：覆盖缺失时回退默认值不报错、配置存在时返回实际值而非默认值。
- [x] 1.4 先编写只读快照测试：覆盖快照不可被运行时修改、修改尝试不影响后续读取。
- [x] 1.5 先编写分离边界测试：覆盖配置读取不触达任何存档键值后端、Bundle 配置加载失败为类型化错误且不产生部分状态。

## 2. 配置服务内核实现

- [x] 2.1 实现 `contracts/config/*` 纯接口：配置键、类型化读取声明、缺失/解析失败/加载失败的类型化错误、只读快照契约。
- [x] 2.2 实现 `core/config/*` 引擎无关服务：不可变配置表、类型化读取、默认值回退、只读快照（冻结结构），使 1.x 测试通过且不依赖 `cc`。
- [x] 2.3 根入口白名单同步：新增配置契约与核心符号至 `expectedRootExports`，依赖边界检查通过。

## 3. Bundle 配置加载适配器

- [ ] 3.1 实现 `adapters/cocos/config/*`：经资源层 `kind: "asset"` 加载配置资源并解析为配置表，复用 `LoadCoordinator`/`ResourceScope` 语义，全程不触达存档后端。
- [ ] 3.2 编写适配器集成测试：真实资源路径下配置可装载并读取、加载失败保留底层原因并以类型化错误呈现。
- [ ] 3.3 完成依赖边界检查与 strict 类型检查：适配器允许 `cc` 导入，内核与契约不导入 `cc`/`fgui`，且不导入存储实现。

## 4. 收口验证

- [ ] 4.1 运行完整 Bun 单元测试与 strict TypeScript 检查，记录测试数量与零失败结果。
- [ ] 4.2 执行 ADR 检查：确认本次配置实现是否产生新的架构决策；如有，按 `doc/decisions/ADR-NNN-<slug>.md` 创建 ADR；如无，明确记录无需新增 ADR。
