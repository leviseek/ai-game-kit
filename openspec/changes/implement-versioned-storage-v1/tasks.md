# Tasks — Implement Versioned Storage v1

## 1. 测试先行

- [x] 1.1 先编写存档测试，覆盖命名空间隔离（互不可见、互不覆盖、删除不影响他命名空间）。
- [x] 1.2 先编写存档测试，覆盖 schema version 写入与读取一致、当前版本免迁移直接读取。
- [x] 1.3 先编写存档测试，覆盖连续多级迁移（v1→v2→v3）与缺失迁移级的类型化错误。
- [x] 1.4 先编写存档测试，覆盖未来版本拒绝（不破坏原数据）与 DTO 不可序列化（undefined/函数/循环引用/BigInt）写入前拒绝。
- [x] 1.5 先编写测试，覆盖仓库通过注入的存储后端与迁移器集合工作，更换后端实现无需修改调用方。

## 2. 契约与错误

- [x] 2.1 实现存储专属类型化错误（未来版本、缺失迁移、不可序列化），沿用框架既有错误机制，携带版本号与缺失迁移路径等诊断上下文。
- [x] 2.2 实现版本化存档仓库契约：读写签名、命名空间参数、schema version、迁移器注册接口，保持引擎无关且不依赖 `PlatformStorage` 之外的类型。

## 3. 核心实现

- [ ] 3.1 实现 DTO 可序列化校验器，递归检测不可序列化值，在写入前拒绝且不产生部分写入。
- [ ] 3.2 实现存档封装记录与存储布局（命名空间作为键前缀，值为 `{ version, data }` 的 JSON 字符串）。
- [ ] 3.3 实现迁移链执行：读取记录 → 未来版本拒绝 → 当前版本直读 → 逐级迁移，任一级缺失或抛错则整体失败并返回类型化错误。
- [ ] 3.4 实现引擎无关的版本化存档仓库，使 1.x 全部测试通过。
- [ ] 3.5 运行 Foundation 完整测试与 strict TypeScript 检查，确认零失败零诊断且不影响既有 417 项测试。

## 4. 收口与 ADR

- [ ] 4.1 审查公开入口，仅导出稳定契约与必要工厂，不导出内部存储结构；运行依赖边界检查确认无深层导入。
- [ ] 4.2 ADR 检查：change 完成前审查本 change 是否产生新的架构决策；如有，按 doc/decisions/ADR-013-<slug>.md 约定创建 ADR；如无，明确记录无需 ADR。
