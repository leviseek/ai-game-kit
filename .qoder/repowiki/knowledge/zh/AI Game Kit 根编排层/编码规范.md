- 所有测试通过 Bun 执行，脚本统一在 package.json scripts 中声明而非直接调用测试框架 CLI
- TypeScript 配置通过 extends 继承 Cocos Creator 生成的 tsconfig，禁止直接修改基础配置
- OpenSpec 变更完成后必须检查并生成对应的 ADR 文档，确保架构决策可追溯
- OpenCode 技能通过 opencode.json 的 mcp.codegraph 配置接入本地 codegraph 服务