- 插件入口按约定式目录组织：commands/ 存放命令定义，skills/ 存放技能脚本，遵循 OpenCode 插件框架的文件约定。
- 通过 package.json 声明对 @opencode-ai/plugin 的版本依赖，而非直接引入源码。
- 运行时产物（node_modules、package-lock.json、bun.lock）统一通过 .gitignore 排除，仅保留源码和依赖声明。