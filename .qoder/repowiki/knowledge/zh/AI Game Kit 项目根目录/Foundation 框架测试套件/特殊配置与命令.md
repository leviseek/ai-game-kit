类型契约检查需设置 `COCOS_TSC` 环境变量指向 Cocos Creator 3.8.8 的 `tsc.js`，或通过 `COCOS_CREATOR_HOME` 自动发现；脚本会排除 `adapters/cocos` 目录，仅对 `assets/framework` 下的 TypeScript 文件执行 `--noEmit --strict --target ES2015 --module ES2015 --moduleResolution node --skipLibCheck` 检查。