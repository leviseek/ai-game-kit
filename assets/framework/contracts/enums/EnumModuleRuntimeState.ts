export enum EnumModuleRuntimeState {
    Registered = "registered",
    Initialized = "initialized",
    Started = "started",
    Paused = "paused",
    Stopped = "stopped",
    Disposed = "disposed",
}
