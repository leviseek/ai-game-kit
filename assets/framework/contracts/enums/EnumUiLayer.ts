/** 七层层级契约，从低到高：scene < normal < popup < guide < toast < loading < system。 */
export enum EnumUiLayer {
    Scene = "scene",
    Normal = "normal",
    Popup = "popup",
    Guide = "guide",
    Toast = "toast",
    Loading = "loading",
    System = "system",
}
